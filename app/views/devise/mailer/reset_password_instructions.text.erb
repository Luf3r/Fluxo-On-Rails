Hello, <%= @resource.name.presence || @resource.email %>.

Someone requested a link to change your Fluxo password. You can do this through the link below.

<%= edit_password_url(@resource, reset_password_token: @token) %>

If you did not request this, please ignore this email.
Your password will not change until you access the link above and create a new one.
