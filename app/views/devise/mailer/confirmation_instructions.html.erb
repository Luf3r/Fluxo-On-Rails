<p>Welcome, <%= @resource.name.presence || @resource.email %>.</p>

<p>You can confirm your Fluxo account email through the link below:</p>

<p><%= link_to "Confirm my account", confirmation_url(@resource, confirmation_token: @token) %></p>
