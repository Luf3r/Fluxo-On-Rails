<p><%= t("auth.mailer.confirmation.greeting", name: @resource.name.presence || @resource.email) %></p>

<p><%= t("auth.mailer.confirmation.body") %></p>

<p><%= link_to t("auth.mailer.confirmation.cta"), confirmation_url(@resource, confirmation_token: @token) %></p>
